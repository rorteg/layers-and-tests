import {ContainerModule, interfaces} from "inversify";
import {container} from "../../Container";

export const demo = new ContainerModule((
    bind: interfaces.Bind,
    unbind: interfaces.Unbind
) => {
    
});